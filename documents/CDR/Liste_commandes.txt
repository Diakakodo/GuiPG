
Indispenssable :

-s, –sign

–clearsign

-b, –detach-sign

-e, –encrypt

-c, –symmetric

–decrypt [=file=]

–verify [[=sigfile=] [=signed-files=]]

–verify-files [=files=]

–list-keys [=names=], –list-public-keys [=names=]

–list-secret-keys [=names=]

–list-sigs [=names=]

–check-sigs [=names=]

–fingerprint [=names=]

–gen-key

–edit-key name

    sign
        
    lsign (2nd)
        
    tsign (2nd)

    revsig
        
    trust
        
    nrsign (2nd)

    disable, enable (2nd)
        
    adduid
        
    deluid
        
    addkey
        
    delkey
        
    revkey 
        
    expire (2nd, A tester : Est ce que ca supprime les signatures ?)
        
    passwd
        
    uid n
        
    key n
         
    check
        
    pref
        
    toggle
        
    save
        
    quit

–sign-key name

–lsign-key name

–trusted-key long key ID

–delete-key name

–delete-secret-key name

–gen-revoke

–export [=names=]

–send-keys [=names=]

–export-all [=names=]

	–export-secret-keys [=names=], –export-secret-subkeys [=names=]

–import [=files=], –fast-import [=files=]

–recv-keys key IDs

–export-ownertrust

–import-ownertrust [=files=]

–print-md algo [=files=] (2nd)

–version

-h, –help

–warranty

Optionnal :

–store

–list-packets

–gen-random 0|1|2 [=count=]

–gen-prime mode bits [=qbits=]

        











